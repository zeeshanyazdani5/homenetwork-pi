# Copy to .env — never commit .env

DATA_DIR=/srv/network/data

# Tailscale
TS_AUTHKEY=tskey-auth-...
TS_HOSTNAME=homenetwork
# Your LAN CIDR for subnet routing (approve in Tailscale admin)
TS_ROUTES=192.168.1.0/24
TS_ACCEPT_DNS=false
